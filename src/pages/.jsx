import React, { useState, useEffect } from 'react';

const  = () => {
    const [state, setState] = useState(null);

    useEffect(() => {
        // TODO: Ajouter logique ici
    }, []);

    return (
        <div className="">
            <h1></h1>
            <p>Ce composant est prêt à être développé.</p>
        </div>
    );
};

export default ;
